const GenderEnum = Object.freeze({"female": "female", "male": "male"});
const RoleEnum = Object.freeze({"standard": "standard", "admin": "admin", "candidate": "candidate"});
export {
    GenderEnum,
    RoleEnum
}